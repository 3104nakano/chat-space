.wrapper
 
  .chat-side  
    .header
      %h3.header__name
        = current_user.name
      %ul.header__menus
        %li.menu
          = link_to new_group_path do
            = fa_icon 'edit', class: 'icon'
        %li.menu
          = link_to edit_user_path(current_user) do
            = fa_icon 'cog', class: 'icon'
    .groups  
      - current_user.groups.each do |group| 
        .group
          .group_name 
            = group.name
          .group_latest-message
            メッセージはまだありません。
     
  .chat-main
    .header
      .header-left
        %h2.header-left__title
          test-group
        %ul.header-left__member-list
          Member:
          %li.header-left__member-list__member
            user name
      .header-right
        .header-right__button
          Edit